import Keyboard from "../components/Keyboard";

export default function GameLayout() {
    return (
        <section className="w-[40rem] flex flex-col items-center justify-center">
            <Keyboard />
        </section>
    )
}